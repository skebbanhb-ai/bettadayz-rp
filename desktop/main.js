const{app,BrowserWindow,shell}=require('electron');const path=require('path');const{fork}=require('child_process');let sp;const PORT=process.env.PORT||'3000';function win(){const w=new BrowserWindow({width:1440,height:900,backgroundColor:'#020617',webPreferences:{preload:path.join(__dirname,'preload.js'),contextIsolation:true,nodeIntegration:false}});w.removeMenu();w.loadURL(`http://127.0.0.1:${PORT}`);w.webContents.setWindowOpenHandler(({url})=>{shell.openExternal(url);return{action:'deny'}})}app.whenReady().then(()=>{sp=fork(path.join(__dirname,'..','server','server.js'),[],{cwd:path.join(__dirname,'..'),env:{...process.env,PORT}});setTimeout(win,1200)});app.on('before-quit',()=>sp&&sp.kill());